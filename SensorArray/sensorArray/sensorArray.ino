/*
 * sensorArray.ino
 *
 * Separate sketch entrypoint that reuses the Sensor Mini-Project template.
 * The included template keeps the same arm pin mapping and uses Servo.h
 * for arm control instead of a bare-metal arm driver.
 */

#include "../sensor_miniproject_template/sensor_miniproject_template.ino"
